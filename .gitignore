__pycache__/
*.pyc
.env
.venv/
venv/
data/raw/
data/ridership/
data/weather/
data/ridership_weather_baseline/
data/batch/
data/debug/
checkpoints/

# Runtime staging generated by live Spark runs. Existing committed sample
# staging remains tracked; these patterns only hide newly generated files.
data/staging/vehicle_positions/
data/staging/trip_delays/
data/staging/speed_layer_delays/
logs/e2e_*/

# Spark Structured Streaming bookkeeping. Bakes the writer's absolute
# path (file:///Users/.../staging/...) into JSON, which breaks downstream
# `spark.read.parquet(<root>)` on any other machine. The Parquet partition
# subdirectories are portable on their own (read with PyArrow/Pandas, or
# `spark.read.parquet("data/staging/vehicle_positions/event_date=*")`).
**/_spark_metadata/
