# Bridge Table Output

`processing/build_bridge_table.py` writes `station_bridge.parquet` here.

The generated bridge table is intended to be shared with Arjun and Yash after raw GTFS/static station data is available and the tiered matching output has been manually reviewed.

